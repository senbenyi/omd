name: network
description: Network module for the melos workspace.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.7.2
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_alice: ^2.0.1
  base:
    path: ../base
  dio: ^5.3.2
  encrypt: ^5.0.1
  unique_identifier: ^0.4.0
  device_info_plus: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
